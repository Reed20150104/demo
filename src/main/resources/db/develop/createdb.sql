CREATE TABLE user (
  id INT PRIMARY KEY,
  username         VARCHAR(30),
  work_id          VARCHAR(30),
  password         VARCHAR(500),
  telephone_number VARCHAR(20),
  mail             VARCHAR(30)
);